<% @posts.each do |post| %>
    <div class="post_wrapper">
        <h2 class="title">
            <%= link_to post.title  %>
        </h2>
        <p class="date">
            <%= time_ago_in_words(post.created_at) %> 
        </p>
    </div>
<% end %>